import { Typography } from "@mui/material";
import React from "react";

export const FakeProfileInvoices: React.FC = () => {
  return (
    <Typography variant="body2">Nothing in here.</Typography>
  );
}

export default FakeProfileInvoices;
