'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getDocumentElement = require('./getDocumentElement.js');
var getComputedStyle = require('./getComputedStyle.js');
var getWindowScrollBarX = require('./getWindowScrollBarX.js');
var getWindowScroll = require('./getWindowScroll.js');
var math = require('../utils/math.js');

// of the `<html>` and `<body>` rect bounds if horizontally scrollable

function getDocumentRect(element) {
  var _element$ownerDocumen;

  var html = getDocumentElement["default"](element);
  var winScroll = getWindowScroll["default"](element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = math.max(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = math.max(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + getWindowScrollBarX["default"](element);
  var y = -winScroll.scrollTop;

  if (getComputedStyle["default"](body || html).direction === 'rtl') {
    x += math.max(html.clientWidth, body ? body.clientWidth : 0) - width;
  }

  return {
    width: width,
    height: height,
    x: x,
    y: y
  };
}

exports["default"] = getDocumentRect;
//# sourceMappingURL=getDocumentRect.js.map
